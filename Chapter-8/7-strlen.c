#include <stdio.h>
#include <string.h> // this library is used for string methods

int main()
{
    char *st = "Arpan";
    int a = strlen(st);
    printf("The length if the string is %d", a);
    return 0;
}