#include <stdio.h>

int main()
{
    // i-- -> phle print fir decrement
    // --i -> phle decrement fir print

    // int i = 5;
    // printf("The value after i-- is %d\n", i--);
    // printf("The value of is %d\n", i);
    // return 0;

    int i = 5;
    printf("The value after --i is %d\n", --i);
    printf("The value of i is %d\n", i);
    return 0;
}