#include <stdio.h>

int main()
{
    // int i = 34;
    // int *a = &i;
    // printf("The value of a pointer before increment is %d\n", a);
    // a = a + 1;
    // printf("The value of a pointer after increment is %d\n", a);
    // return 0;

    // int i = 34;
    // int *a = &i;
    // printf("The value of a pointer before decrement is %d\n", a);
    // a = a - 1;
    // printf("The value of a pointer after decrement is %d\n", a);
    // return 0;

    // char a = 'A';
    // char *b = &a;
    // printf("The value of b pointer before increment is %d\n", b);
    // b = b + 1;
    // printf("The value of b pointer after increment is %d\n", b);
    // return 0;

    // char a = 'A';
    // char *b = &a;
    // printf("The value of b pointer before decrement is %d\n", b);
    // b = b - 1;
    // printf("The value of b pointer after decrement is %d\n", b);
    // return 0;

    // float i = 1.77;
    // float *a = &i;
    // printf("The value of a pointer before increment is %d\n", a);
    // a = a + 1;
    // printf("The value of a pointer after increment is %d\n", a);
    // return 0;

    float i = 1.77;
    float *a = &i;
    printf("The value of a pointer before decrement is %d\n", a);
    a = a - 1;
    printf("The value of a pointer after decrement is %d\n", a);
    return 0;
}