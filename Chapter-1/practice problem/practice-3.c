#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    int height = 3;
    printf("The area of the circle is %f \n", pi * radius * radius);
    printf("The volume of the cylinder is %f", pi * radius * radius * height);

    return 0;
}